# Instagram-App
InstaClone: iOS Instagram rebuild in Swift + SwiftUI using MVVM, Combine, async/await. Firebase Auth for email login, Firestore for users/posts, Storage for image uploads via PhotosPicker (JPEG compression). Features: feed, create/delete posts, likes, comments, follow/unfollow, search, in-app notifications, profile edit, readable timestamps.
